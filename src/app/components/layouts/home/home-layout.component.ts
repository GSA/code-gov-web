import { Component, ViewEncapsulation } from '@angular/core';

@Component({
  selector: 'home-layout',
  encapsulation: ViewEncapsulation.None,
  template: require('./home-layout.template.html'),
})

export class HomeLayoutComponent {
}
