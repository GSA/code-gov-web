describe("Initial spec", function() {

  'use strict';

  it("runs a test", function() {
    expect(':)').toMatch(/.*/);
  });

  xit("skips a test", function() {
    expect(':)').toMatch(/.*/);
  });

//  it("fails another test", function() {
//    expect(':)').not.toMatch(/.*/);
//  });

});
